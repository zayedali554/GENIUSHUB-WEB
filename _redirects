# Netlify redirects file
# Redirect clean URLs to hash-based routing for SPA

/batches    /index.html#batches    200
/live       /index.html#live       200
/secret     /secret.html           200
/player     /player.html           200

# Fallback for any other routes to main page
/*          /index.html            200
